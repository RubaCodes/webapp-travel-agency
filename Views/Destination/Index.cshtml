@{
    ViewData["Title"] = "Destination Page";
}
@model List<Destination>
<div class="text-center">
    @if (Model.Count == 0) {
        <h1>La tua lista delle Destinazioni e' vuota</h1>
    }else{
        <h1>ciao</h1>
    }
</div>